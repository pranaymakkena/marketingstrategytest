{
  "name": "marketing-strategy-test-app-server",
  "version": "1.0.0",
  "description": "Backend server for the marketing strategy test app",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.2",
    // Other backend dependencies
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
    // Other development dependencies
  }
}
